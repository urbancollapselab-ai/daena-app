# Daena v2.0 Dependencies
# Core (zero-dependency for basic operation)
# All stdlib modules used: http.server, json, sqlite3, sched, threading

# MCP Protocol Support (optional but recommended)
mcp[cli]>=1.0

# Semantic Vector Memory (Local, Ultra-fast ONNX based)
fastembed>=0.2.0

# Note: fastembed pulls in onnxruntime and tokenizers.
# It is extremely lightweight compared to PyTorch.
psutil
numpy
faiss-cpu
aiohttp
